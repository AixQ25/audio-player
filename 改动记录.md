# 音频播放器改动记录

## 项目概况

纯 Java 原生 Android 应用，无 XML 布局和第三方依赖。MediaPlayer 后台播放，SharedPreferences 持久化。手动 APK 构建（aapt2 + javac + d8 + apksigner）。

构建环境：Android SDK `D:\Softwear\Android_Studio_sdk`，build-tools 37.0.0，platform android-36.1，JDK `D:\Softwear\Android Studio\jbr`（Java 17）。

---

## 功能改动

### 深色极简 UI 重构

**目标**：应用主要用途是听英文电影音频和跟读字幕，所以 UI 重点从“通用播放器”转向“字幕阅读器”。

**之后**：

- 使用墨黑 / 石墨灰 / 暗金 / 灰绿色配色，减少高饱和颜色对字幕阅读的干扰
- 顶部只保留当前文件收听时长和总收听时长
- 播放控制改为 -30s / -10s / 图标播放键 / +10s / +30s
- 播放键改为自绘圆形图标按钮，不再显示“播放 / 暂停”文字
- 字幕区域成为屏幕视觉中心，导入、备份、文件列表下移
- 倍速和字幕偏移独立成两个低干扰面板
- 强制使用系统无衬线字体，避免手机主题字体破坏阅读界面

**涉及文件**：MainActivity.java、PlaybackService.java、ui-preview/index.html

### 当前字幕聚焦效果

**之前**：当前字幕使用明显边框和背景色，视觉上像一个文本框。

**之后**：当前字幕使用无边界横向渐变光带，只通过柔和背景、文字亮度和轻微阴影提示当前句，避免产生“被框起来”的低级感。

**涉及文件**：MainActivity.java、ui-preview/index.html

### 字幕 UI 重构（从三行固定改为可滚动列表）

**之前**：三个固定的 TextView（上一句 / 当前 / 下一句），通过点击字幕跳转。位置固定，可读性差。

**之后**：ScrollView 包裹 LinearLayout，每次显示当前字幕 ±3 条（共 7 条），形成视觉纵深：

- 当前字幕：22sp，浅色加粗，无边框渐变光带
- 距离 1：15sp，灰色 #64748B
- 距离 2：13sp，浅灰 #94A3B8
- 距离 3+：12sp，最浅 #CBD5E1

当前字幕变化时才重建列表，避免无用刷新。字幕区域自动滚动到当前字幕居中位置。

**涉及文件**：MainActivity.java
**关键改动**：`updateSubtitle()` 完全重写，删除 `subtitlePrev/Current/Next` 三个字段和 `bindSubtitleJump`/`clearSubtitleJumps` 方法，改为 ScrollView + LinearLayout 动态渲染。

### 状态栏重叠修复

**问题**：移除顶部标题后，内容太靠上，和手机状态栏重叠。

**解决**：在根 ScrollView 上调用 `setFitsSystemWindows(true)`，让系统自动处理状态栏内边距。

### 字幕偏移按钮简化

**之前**：延后 0.5s / 延后 0.1s / 重置 / 提前 0.1s / 提前 0.5s

**之后**：-0.5s / -0.1s / 0 / +0.1s / +0.5s，去掉中文，更简洁。

### MP3 列表操作简化

**之前**：每个音频卡片有三个按钮——播放、选择、删除。播放和选择功能重叠。

**之后**：删除"播放"和"选择"按钮，点击卡片直接选中并播放音频。卡片上只保留"删除"按钮。

### 添加字幕移除功能

**之前**：没有移除字幕的途径，导入新字幕也无法覆盖旧的，除非删掉整个音频记录重新导入。

**之后**：已导入字幕的音频卡片上显示"移除字幕"按钮，点击即可解除该音频与字幕的绑定。同时保留偏移量重置。

### 字幕与音频绑定

**说明**：这个功能在第一次开发时就已经实现了。字幕 URI 和文件名保存在 Track 对象中，切换音频时会通过 `parseCurrentSubtitle()` 自动加载对应字幕。导入字幕时也是基于当前选中的音频进行绑定。

### App 名称改为 Vox

**之前**：应用名"英语电影听力"，太长且不够通用。

**之后**：改为"Vox"，简洁，适合音频播放器的定位。

**涉及文件**：`res/values/strings.xml`

### 拔出耳机自动暂停

**问题**：摘掉耳机后音频继续外放，容易尴尬或打扰他人。

**解决**：在 PlaybackService 中注册 `ACTION_AUDIO_BECOMING_NOISY` 广播接收器，耳机/蓝牙断开时自动调用 `pausePlayback()`，保存进度并停止播放。Service 销毁时自动解注册。

**涉及文件**：PlaybackService.java

### 收听数据修正

**说明**：从旧版套壳 app 切换到 Vox 时，收听数据没有同步。直接通过 ADB 修改 SharedPreferences 的 XML 文件，将总收听时长和当前音频文件的已收听时长各增加 6 小时。

### 通知栏 MediaStyle 恢复

**问题**：ExoPlayer 迁移后，通知栏、锁屏、灵动岛（Dynamic Island）的播放控制全部消失。原因是调试阶段移除了 `Notification.MediaStyle`，忘记加回来。

**解决**：在 `buildNotification()` 中添加 `Notification.MediaStyle`，通过 `setMediaSession(mediaSession.getSessionToken())` 关联 MediaSession，并设置 `setShowActionsInCompactView(1, 2, 3)` 显示紧凑模式操作按钮。

**涉及文件**：PlaybackService.java

### VBR MP3 时间偏移根因确认

**问题**：ExoPlayer 迁移后，字幕和音频仍然存在 38 秒偏移。用户反馈“男主的最后一句台词在电脑上大概是 1:24:02，到了手机上成了 1:24:40”。

**根因分析**：问题不在 ExoPlayer——ExoPlayer 的帧级 PTS 是准确的（每帧 24ms，从 0 线性到 5381 秒）。问题出在 MP3 文件本身的 Xing TOC 元数据。该文件由 Bilibili XCoder 处理，LAME tag 错误地将 VBR 文件声明为 CBR（VBR method=1），导致 PC 播放器（VLC）使用字节偏移 / 平均比特率估算时间，显示错误的时间轴。用户手动调整字幕时对齐的是 VLC 的错误时间轴，所以在 ExoPlayer 的正确时间轴上对不上。

**最终解决**：用户将 VBR MP3 转为 CBR 格式，消除比特率变化导致的时间计算分歧，字幕同步恢复正常。这个方案比修正 TOC 元数据更直接有效。

---

## 遇到的问题和解决方式

### Java 匿名内部类引用非 final 变量

**问题**：在 `updateSubtitle()` 中，`currentIndex` 变量在 for 循环中被重新赋值，导致它在匿名内部类（`Runnable`）中引用时不是 effectively final，编译报错。

**解决**：创建一个 `final int scrollIndex = currentIndex` 副本供内部类使用。

### Git 仓库初始化后默认分支名不匹配

**问题**：`git init` 创建的是 `master` 分支，但远程仓库已有 `native` 分支，导致 push 时 ref 不匹配。

**解决**：`git branch -m master native` 重命名本地分支。

### Windows 路径含空格导致 PowerShell 报错

**问题**：`cd f:\vibe coding\...` 不带引号，PowerShell 将空格后的内容解析为额外参数。

**解决**：路径用双引号包裹。

### Git 文件所有权检查失败

**问题**：在不同文件系统间操作时，Git 报 "dubious ownership" 错误。

**解决**：`git config --global --add safe.directory '*'` 全局放行。

### `git push` 时无内容可推送

**问题**：忘记先 `git add` 和 `git commit`，直接 push 导致远端说没有匹配的引用。

**解决**：先 `git add .` 再 `git commit`，最后 `git push`。

### 非 Debuggable 应用无法通过 ADB 修改 SharedPreferences

**问题**：`run-as` 要求应用标记为 debuggable，否则无法访问 `/data/data/` 目录。

**解决**：临时在 AndroidManifest.xml 中添加 `android:debuggable="true"`，重新构建安装（`adb install -r`保留数据），修改完成后再去掉标记重装。修改方式：将文件拉到本地修改后，通过 `run-as cp /data/local/tmp/file /data/data/...` 写回。

### 构建命令说明

每次修改后通过 `build-apk.ps1` 手动构建，流程：

aapt2 compile → aapt2 link → javac → jar → d8 → zipalign → apksigner

然后用 adb install 安装到测试设备（1412ff2d）。

---

## 后续可能的方向

- 字幕可拖拽滚动（不依赖 ScrollView 自带滚动）
- 倍速和偏移设置在备份恢复中完整保留（当前已存储但 UI 上还不够直观）
- 支持更多音频格式（当前只识别 MP3）
- **ExoPlayer 迁移**（已完成，解决 VBR MP3 时间偏差问题）

---

## ExoPlayer 迁移（2026-05-25）

### 背景

VBR（可变比特率）MP3 文件在 MediaPlayer 中时间计算不准。MediaPlayer 通过文件大小除以平均比特率估算位置，VBR 文件各段比特率不同，导致累计偏差可达 37 秒，字幕和音频完全对不上。ExoPlayer 基于帧级时间戳提取，不受比特率变化影响。

### 已完成的工作

**依赖下载**：从 Google Maven 手动下载了 Media3 1.4.1 的 7 个模块（common、container、database、datasource、decoder、extractor、exoplayer），以及 4 个运行时依赖（annotation 1.3.0、collection 1.2.0、core 1.8.0、exifinterface 1.3.6）。额外添加了 Guava 33.0.0-android（Media3 内部函数式接口依赖）。所有 jar 放在 `libs/` 目录。

**PlaybackService 重写**：将 MediaPlayer 调用全部替换为 ExoPlayer API：
- `MediaPlayer` → `ExoPlayer`
- `player.start()` → `player.play()`
- `player.seekTo(int)` → `player.seekTo(long)`
- `player.setPlaybackParams()` → `player.setPlaybackParameters()`
- `player.setVolume(l, r)` → `player.setVolume(v)`
- `player.isPlaying()` → `player.getPlayWhenReady() && state == STATE_READY`
- MediaPlayer 回调 → `Player.Listener`（`onPlaybackStateChanged`、`onPlayerError`）

**构建脚本更新**：`build-apk.ps1` 中 javac classpath 和 D8 输入自动包含 `libs/` 下所有 jar。

### 遇到的问题

1. **D8 路径参数被合并**：PowerShell 嵌套数组 `@(..., $D8Inputs)` 被 D8 误读为单个参数字符串。改用 `+` 拼接扁平数组解决。

2. **Guava 缺失**：构建时有 6 个 desugaring 警告（Function、Predicate、Supplier 等），从 Maven Central 下载 guava-33.0.0-android.jar 解决。

3. **运行时崩溃 `NoClassDefFoundError: CircularIntArray`**：Media3 exoplayer 需要 AndroidX Collection 的 `CircularIntArray` 类。Media3 POM 声明了 4 个运行时依赖（annotation、collection、core、exifinterface）但之前都没下载。补上后崩溃消除。注意 collection 和 annotation 是纯 JAR 而非 AAR，不能只用 `.aar` 扩展名去试。

4. **暂停后无法恢复播放（已解决）**：
   - **症状**：点击 MP3 文件后可以短暂听到声音但立刻停止，点击播放键无反应。Player 卡在 `STATE_BUFFERING`（状态码 2），永远到不了 `STATE_READY`（3）
   - **排查过程**：通过 `Log.wtf` 和 `isActuallyPlaying()` 逐字段日志定位，确认 `player.play()` 被调用且 `playWhenReady=true`，但播放状态始终卡在缓冲态
   - **根因**：`setAudioAttributes(attrs, true)` 的第二个参数让 ExoPlayer 自动管理音频焦点，但 app 自己也在手动管理焦点（`requestAudioFocus()` / `abandonAudioFocus()`）。两边同时管同一件事，当 app 手动放弃焦点时，ExoPlayer 收到焦点丢失通知内部暂停了播放器，之后再 resume 就冲突了
   - **修复**：将 `setAudioAttributes` 的第二个参数从 `true` 改回 `false`，关掉 ExoPlayer 的自动焦点管理，让 app 的手动管理完全接管。日志确认加载、播放、暂停、恢复、seek 全流程正常