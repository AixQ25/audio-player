# Codex 改动记录（2026-05-19）

这份记录只整理本轮在 Codex 中完成的工作过程。中间有一部分界面和功能细节是用户在 Codex 之外自行调整的，这里只作为背景简单带过，不展开记录。

## 1. 需求收敛

最初明确了这个播放器的核心用途：听英文电影音频，而不是做一个复杂的通用音乐播放器。

因此 Codex 后续实现和 UI 设计都围绕几个约束展开：

- 用户自己导入 MP3 文件和字幕文件
- 重点统计当前文件收听时间和总收听时间
- 退出重进后进度必须可靠恢复
- 界面保持简洁，字幕是主角
- 不追求歌单、封面、云同步、推荐等复杂播放器功能

## 2. 原生 Android MVP 阶段

Codex 将原本偏 WebView 壳的方向，推进为纯 Java 原生 Android 实现。

主要建立了这些基础能力：

- `MainActivity.java`：主界面、文件导入、字幕显示、播放控制、数据操作入口
- `PlaybackService.java`：后台播放、前台通知、MediaSession、播放状态广播
- `Track.java` / `TrackStore.java`：音频记录、进度、收听统计、字幕绑定和设置持久化
- `SubtitleParser.java`：解析 SRT / VTT / ASS 字幕
- `TimeFormat.java`：统一时间显示
- `build-apk.ps1`：无 Gradle 的手动 APK 构建脚本

这个阶段完成了播放器的基本闭环：导入 MP3、播放、暂停、跳转、保存进度、统计收听时间、导入字幕并跟随播放显示。

## 3. 安装和测试流程

Codex 整理并实际使用了一套快速测试流程：

```powershell
powershell -ExecutionPolicy Bypass -File "F:\vibe coding\apps\audio-player\音频播放器-native\build-apk.ps1"
& "D:\Softwear\Android_Studio_sdk\platform-tools\adb.exe" install -r "F:\vibe coding\apps\audio-player\音频播放器-native\dist\movie-audio-listener-debug.apk"
& "D:\Softwear\Android_Studio_sdk\platform-tools\adb.exe" shell monkey -p com.aixq.movieaudio -c android.intent.category.LAUNCHER 1
```

每次关键修改后，Codex 都会执行：

- 构建 APK
- ADB 安装到手机
- 启动应用
- 抓取 `logcat`，检查 `AndroidRuntime` / `FATAL`
- 必要时截取手机界面截图做视觉确认

## 4. 播放进度和字幕问题修复

Codex 参与修复了几个影响实际使用体验的问题：

- 退出重进后，播放进度实际存在，但进度条显示从 0 开始
- 字幕显示慢半拍，上一句结束后容易空白，导致声音先到、字幕后到
- 字幕文件导入时可能因为编码或格式差异被判断为无效

修复方向包括：

- 用持久化位置初始化 UI
- 播放状态广播回来后同步进度条和时间
- 调整字幕查找和刷新逻辑
- 增强字幕解析兼容性

## 5. 功能增强阶段

在基础播放器可用后，Codex 按照用户筛选出的优先级加入了几个功能：

- 字幕偏移调节：`-0.5s / -0.1s / 0 / +0.1s / +0.5s`
- 倍速播放：`0.75x / 0.90x / 1.00x / 1.10x / 1.25x`
- 点击字幕跳转到对应时间
- 数据备份与恢复
- 导入和备份按钮下移，让字幕区域更突出
- 快进快退扩展为 `-30s / -10s / +10s / +30s`
- 通知栏播放控制同步支持 `-30 / -10 / 播放暂停 / +10 / +30`

## 6. Codex 之外的中间改动

中间用户对 app 做过一批调整，包括字幕展示方式、界面结构、部分按钮和交互细节等。

这些改动不是本记录重点。Codex 后续先读取了当前代码和已有 `改动记录.md`，在理解现状后继续做 UI 预览和原生迁移，没有回滚这些已有改动。

## 7. HTML UI 预览阶段

用户提出想要深色模式、极简风格、区域分明。Codex 使用 `Build Web Apps` 插件相关能力，先生成独立 HTML 预览：

`ui-preview/index.html`

预览版的结构方向：

- 顶部：当前文件收听时间、总收听时间
- 中部：当前文件名、进度条、播放控制
- 核心区域：字幕阅读区，占据视觉中心
- 下方：倍速、字幕偏移
- 更下方：导入 MP3、导入字幕、备份数据、恢复数据、文件列表

第一版配色偏亮蓝科技感，用户觉得不够高级。Codex 随后改成：

- 墨黑背景
- 石墨灰面板
- 暗金主强调色
- 灰绿色辅助强调色

这套配色最终被用户确认。

## 8. 原生 UI 迁移阶段

用户确认 HTML 预览后，Codex 将设计迁移到 `MainActivity.java`。

主要迁移内容：

- 深色背景和状态栏 / 导航栏颜色
- 顶部统计卡片
- 当前文件行和播放进度
- 五段式播放控制
- 居中的字幕阅读区
- 倍速和字幕偏移面板
- 下置导入、备份、文件列表
- 文件列表卡片的深色样式
- 倍速按钮选中态

迁移后 Codex 通过手机截图发现系统主题字体会把界面变成手写风格，于是增加了字体处理逻辑，优先使用系统无衬线字体文件，例如：

- `/system/fonts/MiSansVF.ttf`
- `/system/fonts/NotoSansCJK-Regular.ttc`
- `/system/fonts/Roboto-Regular.ttf`

## 9. 播放按钮改成图标

用户指出手机上的播放键显示“播放 / 暂停”文字，不如预览版高级。

Codex 随后将播放键从普通 `Button` 改成自绘 `View`：

- `PlayPauseButton`
- 播放中绘制双竖线暂停图标
- 暂停时绘制三角播放图标
- 按钮仍保留 `contentDescription`，但界面不显示文字

这样手机端和预览版保持一致。

## 10. 当前字幕高亮改成无边界光带

用户认为当前字幕用文本框框起来显得低级。Codex 先在 HTML 预览中给出方案：

- 不使用明显边框
- 不使用文本框式背景
- 用横向渐变光带突出当前句
- 中间微亮，左右渐隐
- 文字保留轻微亮度和阴影

用户进一步指出上下边界线仍太明显。Codex 移除上下线，只保留柔和的中间光带。

确认后，Codex 将该效果迁移到 Android：

- 当前字幕背景改为 `LEFT_RIGHT` 渐变
- 去掉边框 stroke
- 增加轻微文字阴影
- 保留字幕点击跳转能力

## 11. 验证记录

最终版本经过了以下验证：

- `build-apk.ps1` 构建通过
- APK 签名验证通过
- ADB 安装到设备 `1412ff2d`
- 启动应用成功
- `logcat` 未发现 `AndroidRuntime` / `FATAL`
- 手机截图确认：
  - 深色 UI 已生效
  - 播放键为图标
  - 当前字幕为无边界渐变光带
  - 倍速和字幕偏移区域位置正常

验证截图路径：

`dist/movieaudio-ui.png`

## 12. Git 同步过程

Codex 将本次主要改动提交为：

```text
6ffd02a 重构播放器深色字幕界面
```

提交内容包括：

- `MainActivity.java`
- `PlaybackService.java`
- `ui-preview/index.html`
- `.gitignore`
- `改动记录.md`

第一次 push 时 GitHub HTTPS 凭据缺失，出现：

```text
SEC_E_NO_CREDENTIALS
```

之后仓库状态显示 `native` 与 `origin/native` 已在同一提交，说明远端追踪分支已同步到该提交。

## 13. 当前状态

截至这份记录创建时，播放器已经从功能可用阶段进入了更稳定的原生体验阶段：

- 核心功能围绕英文电影听力场景收敛
- 退出重进后的播放进度可靠性已改善
- 字幕阅读成为界面中心
- UI 风格已从临时工具界面转为深色极简阅读器风格
- 原生 Android 代码和 HTML 预览已保持同一设计方向
